Move notes to a Google doc
--------------------------

# Using the utility pod:

FIRST_POD=$(kubectl get pods -l app=network-multitool -o=name -n deployed | head -1)
kubectl -n deployed exec -it $FIRST_POD -- bash
curl http://localhost:1180

# .NET to single file and fix build warning
# Make this work locally also
https://docs.microsoft.com/en-us/dotnet/core/deploying/single-file


# Mention how to view certs
kubectl -n deployed get certificate finalapi-cert -o yaml

# Errors loading
Do a kubectl describe on the pod in the deployed namespace

# Useful kind and docker link:
https://iximiuz.com/en/posts/kubernetes-kind-load-docker-image/

# Restarts
https://github.com/kubernetes-sigs/kind/issues/148
docker start kind-1-control-plane && docker exec kind-1-control-plane sh -c 'mount -o remount,ro /sys; kill -USR1 1'

# Troubleshooting

kubectl describe pod/xxx -n deployed
kubectl get events -n deployed